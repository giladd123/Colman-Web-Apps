<head>
  <meta charset="UTF-8" />
  <title>Netflix</title>
  <link rel="icon" href="/images/favicon.ico" />
  <!-- Framework CSS first -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <!-- Page specific CSS (passed via extraCss) -->
  <% if (typeof extraCss !== "undefined") { %>
    <%- extraCss %>
  <% } %>
</head>